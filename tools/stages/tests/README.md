"""MOVED. These tests now live at tests/test_stage_naming.py, next to the rest of
the suite, so `make test` / `pytest` collects them with everything else.

    python3 -m pytest tests/test_stage_naming.py -q

This file is intentionally empty of tests; keeping a second copy under
tools/stages/tests/ would let the two drift, which is the defect class the audit
found throughout this repo.
"""
